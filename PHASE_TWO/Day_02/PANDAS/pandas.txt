NumPy arrays are powerful but limited — 
every column must be the same data type, 
there are no column names, 
and working with mixed data 
(numbers, strings, dates together) is painful.
Pandas solves this with two structures: 
the Series (one column) and the DataFrame (a full table). 
Think of a DataFrame as an Excel spreadsheet inside Python — 
