-- Returns exactly one number, used anywhere a single value is expected



-- Orders above the average amount

SELECT
    order_id,
    customer_id,
    product
FROM orders
WHERE amount > (SELECT DISTINCT AVG(amount) FROM orders);



