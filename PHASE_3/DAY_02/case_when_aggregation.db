This is one of the most frequently tested SQL patterns in interviews.
It lets you turn rows into columns.



-- How many orders of each status per product?

-- Without CASE WHEN — you get a long table

SELECT 
    status,
    product,
    COUNT(*) as total
FROM orders
GROUP BY product, status;



-- With CASE WHEN — you get a wide, readable summary

