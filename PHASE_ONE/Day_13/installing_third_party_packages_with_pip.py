# In this we will see how to install some some third party packages with pip

# ── requirements.txt — professional standard ─────────────────────
# Every real project has this file!

# requirements.txt:
# numpy==1.24.0
# pandas==2.0.0
# matplotlib==3.7.0
# requests==2.31.0

# Install everything at once:
# pip install -r requirements.txt

# ── Using requests (HTTP calls) ───────────────────────────────────
import requests     # pip install requests
